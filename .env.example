# Rum environment configuration template.
# Copy to .env (which is git-ignored) and adjust as needed.
# These are read by backend/internal/pkg/config (envOr "APP_NAME" / "VERSION").

APP_NAME="Rum"
VERSION="1.0.0"
