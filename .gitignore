*.txt
.idea
cmd/swiftget/swiftget
cmd/rum/Rum
/Rum
/rum
.VSCodeCounter
cmd/rum/build.sh
frontend/dist
frontend/node_modules
frontend/package.json.md5
build

# Ignore Wails build output, but keep the committed icon/metadata source assets
# so a fresh clone builds with the real app icon (not the Wails default).
/build/*
!/build/appicon.png
!/build/icon.png
!/build/windows
/build/windows/*
!/build/windows/icon.ico
compressed
Rum

# Local environment files (may contain secrets); keep .env.example tracked
.env
.env.local
!.env.example