*.txt
.idea
cmd/swiftget/swiftget
cmd/rum/Rum
.VSCodeCounter
cmd/rum/build.sh
frontend/dist
frontend/node_modules

# Ignore Wails build output, but keep the committed icon/metadata source assets
# so a fresh clone builds with the real app icon (not the Wails default).
/build/*
!/build/appicon.png
!/build/icon.png
!/build/windows
/build/windows/*
!/build/windows/icon.ico